import { ImagemFilmePipe } from './imagem-filme.pipe';

describe('ImagemFilmePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagemFilmePipe();
    expect(pipe).toBeTruthy();
  });
});
